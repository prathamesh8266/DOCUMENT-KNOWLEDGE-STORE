1. deploy istio, use the README.md present in charts/istio folder
2. deploy postgres (cloudnative)
3. deploy argocd (platform\argocd) 
4. configure the ArgoCd git repo (gitops\bootstrap\repository.yaml)
5. Create the AppProject for argocd
6. deploy SeaweedFS using the helm chart